import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('NovidadesPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NovidadesPage(title: 'Novidades')));
    //  final titleFinder = find.text('Novidades');
    //  expect(titleFinder, findsOneWidget);
  });
}
